import type { Hymn, SacramentMeeting } from "@/lib/types";
function HymnLine({ label, hymn }: { label: string; hymn: Hymn }) { return <p><span className="font-semibold">{label}:</span> {hymn.number ? `${hymn.number} — ${hymn.title}` : hymn.title}</p>; }
export function MeetingDetail({ meeting }: { meeting: SacramentMeeting }) { const date = new Intl.DateTimeFormat("en-US", { weekday: "long", month: "long", day: "numeric", year: "numeric" }).format(new Date(`${meeting.date}T12:00:00`)); return <article className="mx-auto max-w-3xl rounded-xl bg-white p-6 shadow-sm ring-1 ring-slate-200 sm:p-10"><header className="border-b border-slate-200 pb-6 text-center"><p className="text-sm font-semibold uppercase tracking-widest text-teal-700">{meeting.meetingType} meeting</p><h1 className="mt-2 text-3xl font-bold">Sacrament Meeting</h1><p className="mt-2 text-slate-600">{date}</p><p className="mt-4 text-sm text-slate-600">Presiding: {meeting.presiding} · Conducting: {meeting.conducting}</p></header><div className="space-y-7 py-7"><section><h2 className="text-xl font-bold">Opening</h2><div className="mt-3 space-y-2 text-slate-700"><HymnLine label="Opening hymn" hymn={meeting.openingHymn} /><p><span className="font-semibold">Opening prayer:</span> {meeting.openingPrayer}</p></div></section>{meeting.announcements?.length ? <section><h2 className="text-xl font-bold">Announcements</h2><ul className="mt-3 list-disc space-y-1 pl-5 text-slate-700">{meeting.announcements.map((item) => <li key={item}>{item}</li>)}</ul></section> : null}{meeting.wardBusiness.length || meeting.stakeBusiness ? <section><h2 className="text-xl font-bold">Business</h2><ul className="mt-3 list-disc space-y-1 pl-5 text-slate-700">{meeting.wardBusiness.map((item) => <li key={item.description}>{item.description}</li>)}{meeting.stakeBusiness ? <li>Stake business</li> : null}</ul></section> : null}<section><h2 className="text-xl font-bold">Sacrament</h2><div className="mt-3 text-slate-700"><HymnLine label="Sacrament hymn" hymn={meeting.sacramentHymn} /></div></section><section><h2 className="text-xl font-bold">Program</h2><div className="mt-3 space-y-3 text-slate-700">{meeting.speakers.map((item) => <div key={`${item.name}-${item.topic}`}><p className="font-semibold">{item.type === "musical-number" ? "Musical number" : item.name}</p><p>{item.type === "musical-number" ? `${item.name}: ${item.topic}` : item.topic}</p></div>)}</div></section><section><h2 className="text-xl font-bold">Closing</h2><div className="mt-3 space-y-2 text-slate-700"><HymnLine label="Closing hymn" hymn={meeting.closingHymn} /><p><span className="font-semibold">Closing prayer:</span> {meeting.closingPrayer}</p></div></section></div></article>; }
